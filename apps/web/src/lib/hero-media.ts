/** Hero LCP asset — poster is required in public/images/generated/ */
export const HERO_POSTER_SRC = "/images/generated/hero-poster.jpg";

/**
 * Background loop video. Enable only after `public/video/hero-loop.mp4` is deployed.
 * While disabled, hero uses optimized poster image (fixes console 404 + LCP).
 */
export const HERO_VIDEO_ENABLED = false;
export const HERO_VIDEO_SRC = "/video/hero-loop.mp4";
